import {DoublyLinkedList, IElement} from "./DoublyLinkedList";

const doublyLinkedList = new DoublyLinkedList();

const n1 = doublyLinkedList.addLast(10);
const n2 = doublyLinkedList.addLast(20);
const n3 = doublyLinkedList.addLast(30);

console.log("")